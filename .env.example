# Required Environment Variables (must be set in production)
# ==============================================

# MongoDB Connection URI (required)
# For MongoDB Atlas (recommended for production):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/coding_assistant?retryWrites=true&w=majority
# For local development:
MONGO_URI=mongodb://localhost:27017/coding_assistant

# Security Configuration (required)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-this-to-a-secure-random-string-in-production

# Model Configuration (required)
MODEL_PATH=/opt/render/project/src/models/phi-3-mini-4k-instruct-q4.gguf
MODEL_URL=https://huggingface.co/microsoft/Phi-3-mini-4k-instruct-gguf/resolve/main/Phi-3-mini-4k-instruct-q4.gguf

# Authentication Configuration (required for production)
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# Optional Environment Variables (have defaults)
# ==============================================

# Server Configuration
PORT=8000
WORKERS=2  # Number of Gunicorn workers
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Rate Limiting
ENABLE_RATE_LIMITING=true
RATE_LIMIT_CHAT=30/minute
RATE_LIMIT_HISTORY=10/minute
RATE_LIMIT_SAVE=20/minute
RATE_LIMIT_SUGGEST=10/minute

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Production-only Variables (recommended)
# =====================================
# SENTRY_DSN=  # Error tracking
# STATSD_HOST=  # Metrics collection
# NEW_RELIC_LICENSE_KEY=  # Application monitoring
# SSL_CERT_PATH=  # SSL certificate path
# SSL_KEY_PATH=  # SSL private key path